import VerificationSendForm from '@/components/auth/verification-send-form';

const EmailVerificationSend = () => {
  return <VerificationSendForm />;
};

export default EmailVerificationSend;
